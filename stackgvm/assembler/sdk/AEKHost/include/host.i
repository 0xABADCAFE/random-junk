#ifndef _AEKHOST_I_
    // Host functions
    #define host_print_header 1
    #define host_print_rgb    2

    // Useful macros
    #define vec3(x, y, z) x, y, z
    #define vec3_x(v) (v + 0)
    #define vec3_y(v) (v + 1)
    #define vec3_z(v) (v + 2)
#endif
